#!/bin/bash

# Call the VSCode theme setter with Cursor-specific parameters
mateos-theme-set-vscode cursor "$HOME/.config/Cursor/User/settings.json" "$HOME/.local/state/mateos/toggles/skip-cursor-theme-changes" Cursor

